# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`taming-losses-gently`) currently contains no commits and no files — it is an empty
GitHub repository with only a remote configured. There is no source code, build system, test suite,
or documented convention to describe yet.

## Guidance for future instances

- Do not assume a language, framework, or architecture — none has been established.
- Before writing code, check with the user (or the task description) for the intended purpose and
  stack of the project. The repository name suggests it may relate to loss functions / ML training,
  but this has not been confirmed by any file in the repo.
- Once real source files, a build/test setup, and conventions exist, update this file with:
  - Commands to build, lint, test, and run a single test.
  - The high-level architecture (major modules/packages and how they interact).
  - Any non-obvious project-specific conventions.
